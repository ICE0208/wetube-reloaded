extends ../base
include ../mixins/video

block content
    div.profile__data
        img(src=loggedInUser.avatarUrl).avatar
        h3=user.name
    if videos.length !== 0
        div.video-grid
            each info in videos
                +video(info)
    else
        span.empty__message No videos found